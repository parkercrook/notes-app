📌 Phase 1: Basic Note Taking (No DB)
 Set up Flask and routes

 - Build a form to add a note (title + content)
 - Store notes in a Python list (temporarily, in memory)

📌 Phase 2: Add Templates

 - Use Jinja2 to render a notes list
 - Use Bootstrap for basic styling

📌 Phase 3: Database Integration

 - Use SQLite + SQLAlchemy
 - Create a Note model (id, title, content)
 - Store and retrieve notes from DB

📌 Phase 4 (Optional): Authentication

 - Add login/register with password hashing
 - Each user sees only their notes